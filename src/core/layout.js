// Bandera compartida: indica que la escena debe volver a dibujarse porque
// cambió el tamaño de la pantalla (por ejemplo, al rotar el dispositivo).
// Si hay un puzzle abierto esperamos a que se cierre para no perder el avance.
export const layout = { pendiente: false };
